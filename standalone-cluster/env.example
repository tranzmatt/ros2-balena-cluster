# ROS2 Standalone Cluster Node Configuration
# Copy this file to .env and modify as needed

# =============================================================================
# REQUIRED: Discovery Server
# =============================================================================
# IP address and port of the FastDDS Discovery Server (your desktop)
ROS_DISCOVERY_SERVER=192.168.100.100:11811

# =============================================================================
# Node Identity
# =============================================================================
# Unique name for this node (defaults to hostname if not set)
# Use only alphanumeric characters and underscores
NODE_ID=pi5_standalone

# What role this node plays in the cluster
# Options:
#   idle          - Container stays running for manual commands
#   talker        - Publishes text messages to /cluster_chat
#   listener      - Listens to /cluster_chat messages
#   status        - Monitors message stats from all talkers
#   both          - Runs both talker and listener
#   camera        - Publishes webcam images to /cluster_camera/compressed
#   camera_viewer - Receives and tracks images from all cameras
#   camera_talker - Camera + text messages
#   full          - Camera + talker + listener
#   demo_talker   - ROS2 demo talker node
#   demo_listener - ROS2 demo listener node
NODE_ROLE=idle

# =============================================================================
# ROS2 Settings (usually don't need to change)
# =============================================================================
ROS_DOMAIN_ID=0
RMW_IMPLEMENTATION=rmw_fastrtps_cpp
ROS_AUTOMATIC_DISCOVERY_RANGE=SUBNET

# Set to 'true' for full cluster visibility (can see all nodes)
ROS_SUPER_CLIENT=false

# =============================================================================
# Camera Settings (only used when NODE_ROLE involves camera)
# =============================================================================
# Force a specific video device index (leave empty for auto-detect)
CAMERA_DEVICE=

# Capture resolution
CAMERA_WIDTH=640
CAMERA_HEIGHT=480

# Frames per second to publish (lower = less bandwidth)
CAMERA_FPS=1.0

# JPEG compression quality (1-100, higher = better quality, larger files)
CAMERA_JPEG_QUALITY=80

# =============================================================================
# Image Viewer Settings (only used when NODE_ROLE=camera_viewer)
# =============================================================================
# Save received images to disk
SAVE_IMAGES=false

# Directory to save images (inside container)
SAVE_PATH=/shared/images

# Save every N frames (1 = save all, 10 = save every 10th frame)
SAVE_INTERVAL=10
